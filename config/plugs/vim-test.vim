if exists('g:plugs["vim-test"]')

  let test#strategy = "neovim"
  " let test#strategy = {
  "       \ 'nearest': 'neovim',
  "       \ 'file': 'neovim',
  "       \ 'suite': 'basic',
  "       \}

  
endif
